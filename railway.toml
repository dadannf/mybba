[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "apache2-foreground"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[env]
# Environment variables akan diset di Railway Dashboard
# Required:
# - DB_HOST
# - DB_DATABASE
# - DB_USERNAME
# - DB_PASSWORD
# - APP_ENV=production
