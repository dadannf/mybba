# MyBBA - Railway Production Environment Variables
# Copy these variables to Railway Dashboard → Your Project → Variables
# DO NOT commit this file with actual credentials!

# ===========================
# APPLICATION SETTINGS
# ===========================
APP_NAME=MyBBA
APP_ENV=production
APP_KEY=base64:kiJHPbmDEipFgO7OWCUV6f3EX4qPqEv5PMEeXlwxOd8=
APP_DEBUG=false
APP_URL=https://your-app-name.up.railway.app

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file

BCRYPT_ROUNDS=12

# ===========================
# LOGGING
# ===========================
LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=error

# ===========================
# DATABASE (Railway MySQL)
# ===========================
# Get these values from Railway MySQL Service
# Railway Dashboard → MySQL Service → Connect → Copy credentials
DB_CONNECTION=mysql
DB_HOST=${MYSQLHOST}
DB_PORT=${MYSQLPORT}
DB_DATABASE=${MYSQLDATABASE}
DB_USERNAME=${MYSQLUSER}
DB_PASSWORD=${MYSQLPASSWORD}

# Alternative: Manual values (if not using Railway variables)
# DB_HOST=containers-us-west-xxx.railway.app
# DB_PORT=3306
# DB_DATABASE=railway
# DB_USERNAME=root
# DB_PASSWORD=your-mysql-password-from-railway

# ===========================
# SESSION
# ===========================
SESSION_DRIVER=file
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

# ===========================
# CACHE & QUEUE
# ===========================
BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database
CACHE_STORE=file

# ===========================
# REDIS (Optional - if you add Redis service)
# ===========================
# REDIS_CLIENT=phpredis
# REDIS_HOST=${REDISHOST}
# REDIS_PASSWORD=${REDISPASSWORD}
# REDIS_PORT=${REDISPORT}

# ===========================
# MAIL (Use Railway if needed)
# ===========================
MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=smtp.mailtrap.io
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="noreply@mybba.com"
MAIL_FROM_NAME="${APP_NAME}"

# ===========================
# AWS S3 (Optional - for file uploads)
# ===========================
# Use S3 or Cloudflare R2 for persistent file storage
# Railway filesystem is ephemeral (files deleted on redeploy)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"

# ===========================
# NOTES FOR RAILWAY DEPLOYMENT
# ===========================
# 1. Add MySQL service in Railway Dashboard
# 2. Copy all variables above to Railway Dashboard → Variables
# 3. Railway auto-provides: MYSQLHOST, MYSQLPORT, MYSQLDATABASE, MYSQLUSER, MYSQLPASSWORD
# 4. Update APP_URL after first deployment
# 5. For file uploads, consider using S3/R2 (Railway filesystem is ephemeral)
# 6. Monitor logs: railway logs
# 7. Import database: railway run mysql -h $MYSQLHOST -u $MYSQLUSER -p$MYSQLPASSWORD $MYSQLDATABASE < database/backups/dbsekolah.sql
